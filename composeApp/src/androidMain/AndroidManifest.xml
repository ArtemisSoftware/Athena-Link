<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" /><!-- because the user wants to see a specific content-->
                <category android:name="android.intent.category.DEFAULT" /> <!-- if multiple apps can satisfy they will appear as a choice -->
                <category android:name="android.intent.category.BROWSABLE" /> <!-- deeplink can be opened from a browser-->

                <!-- https://athenalink.com/item/12-->
                <data android:scheme="https"/>
                <data android:host="athenalink.com"/>
                <data android:pathPrefix="/item/"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" /><!-- because the user wants to see a specific content-->
                <category android:name="android.intent.category.DEFAULT" /> <!-- if multiple apps can satisfy they will appear as a choice -->
                <category android:name="android.intent.category.BROWSABLE" /> <!-- deeplink can be opened from a browser-->

                <!-- olimpus://athenalink.com/item/12-->
                <data android:scheme="olimpus"/>
                <data android:host="athenalink.com"/>
                <data android:pathPrefix="/item/"/>
            </intent-filter>
        </activity>
    </application>

</manifest>